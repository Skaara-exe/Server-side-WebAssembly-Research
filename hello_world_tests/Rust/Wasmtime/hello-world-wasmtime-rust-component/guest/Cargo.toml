[package]
name = "hello-world-wasmtime-rust-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.52.0", default-features = false, features = ["macros"] }